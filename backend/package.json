{
  "name": "@deeix/api",
  "version": "0.4.4-beta.1",
  "private": true,
  "scripts": {
    "dev": "make run",
    "build": "make build",
    "check": "make check-version && make lint",
    "test": "make test",
    "lint": "make lint",
    "lint:fix": "make fmt",
    "clean": "rm -rf ../.cache/deeix-chat"
  }
}
